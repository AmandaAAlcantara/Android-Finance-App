<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobdevportfolio"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SetGoal"
            android:exported="false"/>

        <activity
            android:name=".budget_transaction.DetailedActivity"
            android:exported="false" />
        <activity android:name=".budget_transaction.AddTransactionActivity" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.SignUpActivity"
            android:exported="false" /> <!-- Home Page -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- UserProfile -->
        <activity
            android:name=".login.UserLoginActivity"
            android:exported="false" /> <!-- Profile -->
        <activity
            android:name=".budget_transaction.TransactionActivity"
            android:exported="false" /> <!-- Budget -->
        <activity
            android:name=".graph.GraphView"
            android:exported="false" />
    </application>

</manifest>